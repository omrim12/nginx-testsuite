apiVersion: v1
kind: Pod
metadata:
  name: nginx-pod
  namespace: webserver-{{ kerberos_user }}
  labels:
    app: nginx
spec:
  containers:
  - name: webserver
    image: nginx:latest
    ports:
    - containerPort: 80
